  <div class="main-image" ng-app="CardModule" ng-controller="CardController" ng-cloak>

  <form ng-submit="getAllCardsForClass()" class="navbar-form navbar-right">
    <button type="submit" class="btn btn-success">Get the cards</button>
      <input type="hidden" name="_csrf" value="<%= _csrf %>" />
  </form>

  <input type="text" ng-model="cardname" ng-change="nameChanged()">
  <img ng-src={{card.img}}>
  <img ng-src={{card.imgGold}}>
  </div>
